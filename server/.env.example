# Server port
PORT=5000

# Comma-separated list of allowed CORS origins (leave empty to allow all origins in development)
# Example: CORS_ORIGIN=http://localhost:5173,https://your-production-domain.com
CORS_ORIGIN=
